# OpenWeatherMap
OPENWEATHER_API_KEY=your_openweather_api_key_here

# Vite-exposed (client) API key — prefix with VITE_ so import.meta.env can read it
VITE_OPENWEATHER_API_KEY=your_openweather_api_key_here